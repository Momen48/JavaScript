
// change String to number

let amount = '100'; //counted as String

//amount = parseInt(amount);
//amount = + amount;             /* unary + operator */
//amount = Number(amount);        /*number method or constructor */
amount = parseFloat(amount);     /*String to Decimal*/

console.log(amount, typeof amount);

// Change Number to String 

let num = 99.5;        //counted as Number
//num = num.toString();     //primitive k non pritive di convert korai JS built in Temporart Wrapper create kore.




console.log(num, typeof num);

